# Copy to .env.local for local dev - never commit the real file.
# Points the frontend at the backend API - see backend/.env.example for
# the matching ALLOWED_ORIGIN it needs to accept requests from this origin.
VITE_API_BASE_URL=http://localhost:8080
